<%= form_for([@post, @post.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :commenter %><br />
    <%= f.text_field :commenter %>
  </div>
  
    <%= f.label :body %><br />
    <%= f.text_area :body, {:width=>480} %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>